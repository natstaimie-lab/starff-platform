/** Bundled Starff logo assets (real brand marks). */
export const logos = {
  // White horizontal logo — for dark backgrounds (splash, auth).
  white: require('../../assets/logo-white.png'),
  // Full-colour horizontal logo — for light backgrounds (headers).
  horizontal: require('../../assets/logo-horizontal.png'),
  // The mark on its own.
  mark: require('../../assets/logo-mark.png'),
};

// Native aspect ratio of the horizontal logos (843 x 300).
export const LOGO_ASPECT = 843 / 300;
